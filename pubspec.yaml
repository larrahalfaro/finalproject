name: ecotech
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # iOS-style icons
  cupertino_icons: ^1.0.8

  # --- Firebase Dependencies (Updated for Flutter SDK 3.x) ---
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  provider: ^6.0.5
  # (Optional) You can add these if needed later:
  # firebase_storage: ^12.3.0
  # firebase_messaging: ^15.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # --- Example for adding custom assets ---
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # --- Example for adding custom fonts ---
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: fonts/Inter-Regular.ttf
  #       - asset: fonts/Inter-Bold.ttf
  #         weight: 700
